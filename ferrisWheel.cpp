#include <iostream>
#include <array>

def main() {
    int n {};
    int k {};

    std::cin >> n >> k;

    std::array<int, n> children = {};

    for (int i = 0; i < n; i++) {
        std::cin >> array[i];
    }

    int count {0};
    int heaviestChildIndex {n-1};

    while (heaviestChildIndex >= 0) {
        
    }





    return 0
}